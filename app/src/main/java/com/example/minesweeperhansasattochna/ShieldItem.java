package com.example.minesweeperhansasattochna;

public class ShieldItem {
    public static void activateShield(GameActivity gameActivity) {
        gameActivity.setShieldActive(true); // Assuming a `shieldActive` flag in GameActivity
    }
}
